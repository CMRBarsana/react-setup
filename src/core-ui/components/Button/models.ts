export type ButtonType = 'button' | 'submit';

export interface ButtonThemeType {
  disabled: string;
  primary: string;
  secondary: string;
  info: string;
  success: string;
  warning: string;
  danger: string;
  gray: string;
}
